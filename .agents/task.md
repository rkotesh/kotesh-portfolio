# Tasks

- [x] Prepare `index.html` by adding injection comment markers for projects and experience.
- [x] Prepare `script.js` by adding injection comment markers for `projectDb` and chatbot database.
- [x] Create `scripts/update_portfolio.py` to:
  - [x] Fetch new GitHub repositories and append them to HTML and JS.
  - [x] Fetch labeled GitHub Issues (acting as LinkedIn updates) and append them to Experience/Education.
- [x] Create `.github/workflows/portfolio-updater.yml` to schedule the script daily and commit changes.
- [x] Verify locally that the Python script executes correctly and formats modifications correctly.
